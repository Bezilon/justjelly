"use server";

// import { db } from "@/db";
// import { servers, TNewPost } from "@/db/schema/servers";
// import { revalidatePath } from "next/cache";

// export const createPost = async (data: TNewPost) => {
//   await db.insert(servers).values(data);
//   revalidatePath("/posts");
// };